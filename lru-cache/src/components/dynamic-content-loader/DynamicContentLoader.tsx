import styles from './DynamicContentLoader.module.css';

type Props = {};

const DynamicContentLoader = (props: Props) => {
  return (
    <div>
      <h2>Dynamic Content Loader with LRU Cache</h2>
    </div>
  );
};

export default DynamicContentLoader;
